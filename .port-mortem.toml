# Port Mortem 2026 — Code Resurrection Wave 2
# Entry metadata for decimaljs-go.

track = "F"                                  # JavaScript -> Go
source_language = "JavaScript"
target_language = "Go"

[source]
name = "decimal.js"
repo = "https://github.com/MikeMcl/decimal.js"
commit = "cd73a7f830f07bc98e906d2ebe76e8c02cc20c8f"
version = "10.6.0"
license = "MIT"

[baseline]
# The upstream suite on a clean clone of the commit above, run with
# `npm test` (node ./test/test.js). This is the denominator for parity:
# the port matches decimal.js, not an ideal.
command = "node ./test/test.js"
assertions_total = 22658
assertions_passing = 22658

[kickoff]
# Hash published by the organisers at the start of the window.
# Replace the placeholder with the announced value.
hash = "REPLACE_WITH_ANNOUNCED_KICKOFF_HASH"
